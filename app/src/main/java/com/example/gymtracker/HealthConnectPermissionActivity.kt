package com.example.gymtracker

import androidx.activity.ComponentActivity

/**
 * An empty, translucent activity to handle the Health Connect permission result.
 * The system redirects back to this activity after the user makes a choice on the
 * Health Connect permission screen.
 */
class HealthConnectPermissionActivity : ComponentActivity()